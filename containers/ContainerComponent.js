import React from 'react'
import { connect } from 'react-redux'
import PresentationComponent from '../modules//PresentationComponent'

const mapStateToProps = (state) => {
    return {}
}

const ContainerComponent = connect(mapStateToProps)(ContainerComponent)

export default ContainerComponent
